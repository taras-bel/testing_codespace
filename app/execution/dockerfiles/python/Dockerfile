# Базовый образ 
FROM python:3.9-slim 
 
# Создаем рабочую директорию 
WORKDIR /app 
 
# Копируем скрипт для выполнения (не обязательно, если монтировать) 
# COPY . /app 
 
# Команда по умолчанию будет переопределена командой `docker run` 
CMD ["python", "main.py"]