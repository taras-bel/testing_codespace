function notifyDiscordSessionStart(sessionId, user) {
  console.log(`[Discord] Notifying session start for ${sessionId} by ${user}`);
  // Place for real Discord webhook or bot integration
}
module.exports = { notifyDiscordSessionStart };